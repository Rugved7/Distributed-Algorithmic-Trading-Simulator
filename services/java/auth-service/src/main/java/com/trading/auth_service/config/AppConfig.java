package com.trading.auth_service.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Phase 1 skeleton:
    // Add password encoder, token provider, and interceptor beans in later phases.
}
